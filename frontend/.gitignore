# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
build
*.local
.env
.env.capacitor

# Capacitor Android platform build output (the android/ project itself is
# committed - it carries the geolocation permission and other manifest
# customizations that a bare `cap add android` wouldn't reproduce).
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/app/release/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite-cache/